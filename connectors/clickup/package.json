{
  "name": "clickup-connector",
  "version": "1.0.0",
  "description": "ClickUp API connector for managing workspaces, spaces, folders, lists, and tasks",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2"
  }
}
