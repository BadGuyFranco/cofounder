{
  "name": "huggingface-connector",
  "version": "1.0.0",
  "description": "HuggingFace API connector for running ML models",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
