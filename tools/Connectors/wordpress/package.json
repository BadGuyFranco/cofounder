{
  "name": "wordpress-connector",
  "version": "1.0.0",
  "description": "WordPress REST API connector for managing posts, pages, media, and more",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
